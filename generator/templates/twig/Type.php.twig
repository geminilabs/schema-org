<?php

namespace GeminiLabs\SchemaOrg;

/**
 * {{ type.description | doc(0) }}
 *
 * @see {{ type.resource }}
{% if type.properties %} *
{% for property in type.properties %} * @method static {{ property.name }}( {{ property.ranges | join('|') }} ${{ property.name }} )
{% endfor %}
{% endif %}
 */
class {{ type.name }} extends {{ type.parent }}
{
{% if type.properties %}    /**
     * @see http://schema.org/{PROPERTY_NAME}
     */
    protected static $PROPERTIES = [
{% for property in type.properties %}        '{{ property.name }}',
{% endfor %}
    ];
{% endif %}
}
